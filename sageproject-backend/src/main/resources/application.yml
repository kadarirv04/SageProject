spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sageit_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: Root@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

app:
  jwtSecret: sageItSecretKey123!@#
  jwtExpirationMs: 86400000  # 24 hours

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    org:
      springframework:
        security: INFO
      hibernate: ERROR
